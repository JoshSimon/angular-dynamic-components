export interface FixedPartOfTheDynamicComponent {
    data: any;
}

/* When I declare a variable thats a type I can call variable.data - It will be there because the interface is common for these variables */